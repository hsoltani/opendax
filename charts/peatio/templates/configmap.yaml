{{ if  ( (.Files.Glob "config/plugins.yml") or (.Files.Glob "config/management_api_v1.yml") )}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "peatio.fullname" . }}
  labels: 
    {{- include "peatio.labels" . | nindent 4 }}
    app.kubernetes.io/component: peatio
data:
{{- if (.Files.Glob "config/plugins.yml") }}
{{ (.Files.Glob "config/plugins.yml").AsConfig | indent 2 }}
{{- end }}
{{ end }}
