{{ if  ( (.Files.Glob "config") or (.Files.Glob "config/management_api_v1.yml") )}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "peatio.fullname" . }}
  labels: 
    {{- include "peatio.labels" . | nindent 4 }}
    app.kubernetes.io/component: peatio
data:
{{- if (.Files.Glob "config") }}
{{ (.Files.Glob "config").AsConfig | indent 4 }}
{{- end }}
{{ end }}
